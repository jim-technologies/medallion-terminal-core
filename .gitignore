node_modules
# dist/ is committed so this package is installable directly from git
# (github:jim-technologies/medallion-terminal-core) with no build step. Rebuild
# with `pnpm build:lib` and commit the result when src/ changes.
dist-app
storybook-static
playwright-report
test-results
blob-report
*.tsbuildinfo
*.local
.DS_Store
.claude/

# Defensive: examples/ contains source widgets, but a stray tsc run
# can leak .d.ts outputs. The intentional ambient types use .d.mts.
examples/**/*.d.ts
